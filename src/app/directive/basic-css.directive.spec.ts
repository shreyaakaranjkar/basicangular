import { BasicCssDirective } from './basic-css.directive';

describe('BasicCssDirective', () => {
  it('should create an instance', () => {
    const directive = new BasicCssDirective();
    expect(directive).toBeTruthy();
  });
});
