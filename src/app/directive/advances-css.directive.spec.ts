import { AdvancesCssDirective } from './advances-css.directive';

describe('AdvancesCssDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvancesCssDirective();
    expect(directive).toBeTruthy();
  });
});
